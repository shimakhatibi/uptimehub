name: UptimeHub CD

on:
  workflow_run:
    workflows: ["UptimeHub CI"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deployment will be configured here"
